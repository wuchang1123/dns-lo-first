# Local First - 本地优先 DNS 服务器

基于 Go 开发的高性能 DNS 服务器，支持智能分流、污染检测和定时更新。

- IPv4 Only，AAAA 查询按 IPv4 分流规则转发，本服务不处理，也不记录缓存
- 终端里按 Ctrl+C 退出，停止 DNS 服务器

## Bootstrap DNS

- 配置文件 bootstrap_dns，下载配置解析域名时使用的递归 DNS（Do53，host:port）
    - 本服务刚启动时系统 resolv 可能不可用（如指向 [::1]:53），此处显式指定可避免依赖本机解析
    - 留空则使用配置中的上游 DNS 服务器
        - 必须只取 Do53 的 host:port 并过滤回环
        - 若仍为空则内置 223.5.5.5 / 8.8.8.8 / 1.1.1.1。

## 上游服务器

- 支持 IP（Do53，host:port）
- 支持 DoH
    - 本地类上游的 DoH URL 里的 hostname 解析必须使用 Bootstrap DNS 的流程
    - 海外类上游的 DoH URL 里的 hostname 解析必须使用 doh_bootstrap_dns 配置的上游，并过滤回环地址
- 实现上游服务器打分制
    - 不区分 Do53/DoH，合并按地址记账
    - 最近响应延迟低的分值高
    - 按分值排序使用上游服务器
    - 分数相等的上游服务器随机提取
    - 1小时内有一次从分值最低的（不包括被冻结的）读取的机会
    - 其他具体机制交给大模型来决定
    - 只缓存在内存中
- 上游服务器信誉制
    - 主要是为了识别本地类上游的被污染情况，信誉度高的可以优先使用，为查询海外域名提速
    - 各个上游定义的污染规则不一，所以可以负值
    - 原理
        - ASN 配置文件有定义的域名
            - 对上游服务器的响应结果进行 ASN 校验
                - 信誉度的增减：响应结果 IPs 与域名对应的组织下的 IP 集合有没有交集
        - ASN 配置文件没有定义的域名
            - 不参与信誉制
    - 其他具体机制交给大模型来决定
- 划分为本地和海外两大类
    - 各类按职能分流规则
    - 海外服务响应延迟会比本地的高，评分可以酌情调整缩小差距
- 单个上游服务器超时时间
    - 3 秒，可通过配置修改
- 上游服务器超时处理
    - 冻结 1 分钟
    - 同类型全部冻结时：先解冻时间最早的 ⌈N/2⌉ 个，N 为同类服务器的数量
- 上游服务器提取规则
    - 本地、海外并发请求时
        - 每一类服务器按信誉度先提取一个上游使用（非冻结状态的）
            - 超时，可并发尝试同类剩下的所有上游
            - 所有上游都超时视为失败
    - 只启用一类时
        - 每一类服务器按打分制先提取一个上游使用（非冻结状态的）
            - 超时，可并发尝试同类剩下的所有上游
            - 所有上游都超时视为失败
- EDNS Client Subnet
    - 注入固定值，可配置
- 相同请求合并
    - 按本地/海外划分两类，同类的相同请求合并
        - 查询前预检 in-flight 表，若已有进行中查询则等其结果

## 缓存

- 缓存格式
    - 同时内存和文件存储，内存加速，文件持久化
    - 文件存储时转换成人可读的格式，需要从文件加载时再转换回来
- 基础响应缓存：
    - 负责主要的 DNS 缓存
    - 采取乐观缓存：过期仍返回，响应给客户端的 TTL 固定为 3 秒
        - 返回后仍然在后台执行后续逻辑（客户端该次查询算是已经结束，后面不需要再回复响应）
    - 缓存 key 按 qname + qtype + qclass
    - 需要记录对应响应的上游服务器
    - NXDOMAIN：可写入基础缓存
    - NOERROR + 无可用 A 记录：不能写入基础缓存
    - SERVFAIL/REFUSED/格式错：视为失败，不能写入基础缓存
    - 缓存限制 N 条（可配置）
        - 删除最久未访问的（LRU）
- 响应结果缓存
    - 缓存 key 按 qname + qtype + qclass
    - 只记录海外类上游的有效 A 响应，海外任一有效响应即可写入或累计。
    - NOERROR + 无可用 A 记录：不能写入缓存
    - NXDOMAIN：不能写入缓存
    - SERVFAIL/REFUSED/格式错：视为失败，不能写入缓存
    - 记录响应次数
    - 记录响应结果的 IPs，去重
    - 相同 IP 累计次数
    - 同一 IP 超过 N 天没再出现就从集合中删除（可配置）
    - 响应次数和 IP 累计次数仅用于统计/日志，不参与一致性判断
    - 缓存按 key 限制 N 条（可配置）
        - 超过限制删除最久未访问的（LRU）
- 上游信誉缓存
    - 永不过期
- 基础响应缓存、响应结果缓存各自独立一套，相互不干扰

## 配置文件

- 基础配置文件：@config.yaml
- ASN 配置文件：@data/domain_asn.yaml 域名/组织/IP段映射信息

### local_domains 所在国域名列表
```yaml
local_domains:
  source_url: "https://raw.githubusercontent.com/wuchang1123/cn-dns-conf/refs/heads/main/out/dnsmasq-china.conf"
  file_path: "./data/china_domains.txt"
  update_interval_hours: 24
```

## 智能分流

- 配置文件 local_domains 定义的域名只使用本地类上游
- 配置文件 local_only 定义的域名只使用本地类上游
- local_only > local_domains > default（默认海外）
    - apex + 子域
    - 支持 *.example.com 严格子域（RFC 4592）

## 查询上游服务器的流程

1. 有基础缓存
    - 未过期，立即返回缓存的结果，本次查询结束
    - 已过期
        - 立即返回，ttl=3
        - 返回后在后台执行后续逻辑
2. local_only、local_domains 的域名，只启用本地类上游服务器
    - 一有响应立即返回，同时更新基础缓存，本次查询结束
    - 6s超时，直接 SERVFAIL
3. 余下的域名
    - 如果没有对应的响应结果缓存，则只启用海外类上游服务器
        - 有响应立即返回，同时更新基础缓存、响应结果缓存
        - 6s超时，直接 SERVFAIL
    - 如果有对应的响应结果缓存，则启用本地、海外类上游服务器，并发请求
        - 如果海外服务器先于本地服务器返回（或者同时），那么立即以海外结果响应客户端
            - 同时更新基础缓存、响应结果缓存
        - 如果本地服务器先于海外服务器返回
            - 拿本地结果与对应的响应结果缓存做校对比较
                - 结果一致的条件
                    - IP 集合有交集
                    - 其中有 IP 在相同的 IP 段内：先校验"/24"，失败再校验"/16"
            - 如果校对结果一致，那么先以本地服务器的响应立即响应返回，ttl=3，不更新基础缓存
                - 在后台运行等待海外服务器返回
                    - 返回后以海外结果更新基础缓存、响应结果缓存
            - 不一致并且海外服务器有响应返回时，立即以海外结果响应客户端
                - 同时更新基础缓存、响应结果缓存
        - 6s超时
            - 若已有本地 NOERROR + A 响应，则返回本地响应，TTL=3，不更新基础缓存
            - 若没有任何可用 A 响应，则返回 SERVFAIL

## 日志系统

- 日志时间保留显示精确到 ms
- 日志时间的年份只取最后两位

### 基础日志
    - 日志等级: debug, info, warn, error, fatal
    - 日志采用时区：Asia/Shanghai
    - 详细记录日志，有足够信息方便调试
    - 每天0点清除超过 48 小时的日志（启动时不要）

### 性能日志
    - 耗时统一 ms 单位，取小数点 3 位，四舍五入
    - 日志格式前面统一先显示时间、耗时
    - 耗时记录（独立文件记录 query.log）
        - 打印每个查询从进入到返回所需时间
            - 包含 qname/qtype/qclass、耗时、上游服务器、响应 IP 和 rcode
                - 耗时排在时间后面，域名前
            - 无键值格式输出
        - 打印每次上游查询起止的分段耗时
            - 最少包含上游服务器信息、开始&结束时间（HH:MM:SS.mmm）
    - 每天0点清除超过 48 小时的日志（启动时不要）

## 多端支持

- make脚本，可以编译支持各大平台

## 定时更新

- local_domains 按 update_interval_hours 更新。
- 启动时文件不存在则立即下载。
- 下载成功后热加载。
- 下载失败保留旧文件/旧内存规则。
